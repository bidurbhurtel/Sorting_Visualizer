import React from 'react'
import { Button } from 'react-bootstrap'

const GenNewArray = () => {
    return (
        <div>
            <Button variant='dark' onClick={changeArray}></Button>
        </div>
    )
}

export default GenNewArray
